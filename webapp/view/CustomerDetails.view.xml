<mvc:View
    controllerName="com.ifb.invoicegenerator.controller.CustomerDetails"
    xmlns="sap.m"
    xmlns:mvc="sap.ui.core.mvc"
    xmlns:l="sap.ui.layout"
    xmlns:f="sap.ui.layout.form"
    xmlns:core="sap.ui.core"
>
    <Page showHeader="true"
    class="sapUiContentPadding sapUiResponsivePadding--header sapUiResponsivePadding--subHeader sapUiResponsivePadding--content sapUiResponsivePadding--footer">
        <headerContent>
            <core:Fragment
                fragmentName='com.ifb.invoicegenerator.fragments.Menu'
                type='XML'
            />
        </headerContent>

        <content>
            <VBox class="sapUiSmallMargin">
                <f:SimpleForm
                    id="SimpleFormDisplayCustomer"
                    editable="false"
                    layout="ResponsiveGridLayout"
                    labelSpanXL="3"
                    labelSpanL="3"
                    labelSpanM="3"
                    labelSpanS="12"
                    adjustLabelSpan="false"
                    emptySpanXL="4"
                    emptySpanL="4"
                    emptySpanM="4"
                    emptySpanS="0"
                    columnsXL="1"
                    columnsL="1"
                    columnsM="1"
                    singleContainerFullSize="false"
                >
                <f:toolbar>
                	<Toolbar>
					<Title text="Customer"/>
					<ToolbarSpacer />
					<Button text="Deactivate" icon="sap-icon://cancel"
                        visible="{= (${LoginDataModel>/usertype} === 'A') ? true : false }" press="onDeactivateCustomer"/>
				</Toolbar>
                </f:toolbar>
                    <f:content>
                        <Label text="Branch" />
                        <Text
                            text="{CustomerDetailModel>/branchname} ({CustomerDetailModel>/branchcode})"
                        />
                        <Label text="Customer Address" />
                        <Text text="{CustomerDetailModel>/address}" />
                        <Label text="Pin Code" />
                        <Text text="{CustomerDetailModel>/pincode}" />
                        <Label text="GSTN Number" />
                        <Text text="{CustomerDetailModel>/gstinnum}" />
                        <Label text="PAN" />
                        <Text text="{CustomerDetailModel>/pannum}" />
                        <Label text="Mobile Number" />
                        <Text text="{CustomerDetailModel>/mobilenum}" />
                        <Label text="Email ID" />
                        <Text text="{CustomerDetailModel>/emailid}" />
                        <Label text="Region" />
                        <Text
                            text="{CustomerDetailModel>/regiondesc} ({CustomerDetailModel>/regioncode})"
                        />
                    </f:content>
                </f:SimpleForm>
            </VBox>
        </content>
    </Page>
</mvc:View>
