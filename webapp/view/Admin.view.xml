<mvc:View
    controllerName="com.ifb.invoicegenerator.controller.Admin"
    displayBlock="true"
    xmlns="sap.m"
    xmlns:l="sap.ui.layout"
    xmlns:f="sap.ui.layout.form"
    xmlns:mvc="sap.ui.core.mvc"
    xmlns:core="sap.ui.core"
>
    <Page
        showHeader="true"
        backgroundDesign="Transparent"
        title="Dashboard"
    >
        <headerContent>
            <core:Fragment
                fragmentName='com.ifb.invoicegenerator.fragments.Menu'
                type='XML'
            />
        </headerContent>
        <content>
            <GenericTile
                visible="{= (${LoginDataModel>/usertype} === 'A' || ${LoginDataModel>/usertype} === 'C') ? true : false }"
                class="sapUiTinyMarginBegin sapUiTinyMarginTop tileLayout"
                header="Materials"
                press="onMaterialPress"
            >
                <TileContent>
                    <ImageContent src="sap-icon://product" />
                </TileContent>
            </GenericTile>
            <GenericTile
                visible="{= (${LoginDataModel>/usertype} === 'A' || ${LoginDataModel>/usertype} === 'V') ? true : false }"
                class="sapUiTinyMarginBegin sapUiTinyMarginTop tileLayout"
                header="{= (${LoginDataModel>/usertype} === 'V') ? 'My Branch' : 'Customers' }"
                press="onBranchUserPress"
            >
                <TileContent>
                    <ImageContent src="sap-icon://company-view" />
                </TileContent>
            </GenericTile>
            <GenericTile
                visible="{= (${LoginDataModel>/usertype} === 'A' || ${LoginDataModel>/usertype} === 'C') ? true : false }"
                class="sapUiTinyMarginBegin sapUiTinyMarginTop tileLayout"
                header="Franchisee"
                press="onFranchiseePress"
            >
                <TileContent>
                    <ImageContent src="sap-icon://customer" />
                </TileContent>
            </GenericTile>
            <GenericTile
                visible="{= (${LoginDataModel>/usertype} === 'A') ? true : false }"
                class="sapUiTinyMarginBegin sapUiTinyMarginTop tileLayout"
                header="App Users"
                press="onAppUsersPress"
            >
                <TileContent>
                    <ImageContent src="sap-icon://employee" />
                </TileContent>
            </GenericTile>
            <GenericTile
                visible="{= (${LoginDataModel>/usertype} === 'C') ? true : false }"
                class="sapUiTinyMarginBegin sapUiTinyMarginTop tileLayout"
                header="Sync Data"
                subheader="for Invoice Generation"
                press="onSyncInvoiceGenerationPress"
            >
                <TileContent>
                    <ImageContent src="sap-icon://synchronize" />
                </TileContent>
            </GenericTile>

            <GenericTile
                visible="{= (${LoginDataModel>/usertype} === 'V' || ${LoginDataModel>/usertype} === 'C') ? true : false }"
                class="sapUiTinyMarginBegin sapUiTinyMarginTop tileLayout"
                header="Invoice Management"
                subheader="Sales to IFB"
                press="onSalesTransactionsPress"
            >
                <TileContent>
                    <ImageContent src="sap-icon://sales-order" />
                </TileContent>
            </GenericTile>

            <GenericTile
                visible="{= (${LoginDataModel>/usertype} === 'V') ? true : false }"
                class="sapUiTinyMarginBegin sapUiTinyMarginTop tileLayout"
                header="Digital Signatures"
                subheader="of Invoices"
                press="onDigitalSignaturePress"
            >
                <TileContent>
                    <ImageContent src="sap-icon://signature" />
                </TileContent>
            </GenericTile>
            <GenericTile
                visible="{= (${LoginDataModel>/usertype} === 'A') ? true : false }"
                class="sapUiTinyMarginBegin sapUiTinyMarginTop tileLayout"
                header="Sync Master Data"
                press="onMasterDataSyncPress"
            >
                <TileContent>
                    <ImageContent src="sap-icon://synchronize" />
                </TileContent>
            </GenericTile>
        </content>
    </Page>
</mvc:View>
